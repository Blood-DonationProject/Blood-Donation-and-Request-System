# Plan: Add Myanmar + English Bilingual Support to All Pages

## Current State
- **Framework**: Plain PHP + Tailwind CSS (CDN)
- **i18n system**: Already exists (`assets/js/translations.js` + `assets/js/i18n.js`)
- **Language switcher**: EN/MY dropdown already on most pages
- **Problem**: Many pages are missing `data-i18n` attributes, and the Myanmar translations in `translations.js` are placeholder/duplicate text (not real Burmese translations)

## Scope of Work

### Step 1: Rewrite `assets/js/translations.js`
Replace the entire Myanmar (`my`) section with accurate Burmese translations for ALL keys. The current `my` section has ~40% duplicate/placeholder values.

**Files to modify**: `assets/js/translations.js`

### Step 2: Add missing translation keys
Add new keys for text that currently has no `data-i18n` attribute, covering:
- `register.php` (full form: Create Account, Full Name, Email Address, Phone Number, Password, Confirm Password, etc.)
- `login.php` (Welcome Back to BloodLife, Track Your Impact, Emergency Alerts, Earn Rewards, etc.)
- `becomedonor.php` (all section headings and descriptions)
- `donateform.php` (all form labels)
- `donordashboard.php` (all dashboard text)
- `profile.php` (all profile sections)
- Admin pages (`dashboard.php`, `donors.php`, `hospitals.php`, `requests.php`, etc.)

### Step 3: Add `data-i18n` attributes to pages missing them

**User pages (8 files)**:
| File | Status | Work needed |
|------|--------|-------------|
| `user/index.php` | ✅ Good | Minor gaps (stats section labels) |
| `user/donor.php` | Partial | Add attributes to remaining text |
| `user/hospital.php` | Partial | Add attributes to remaining text |
| `user/bloodrequest.php` | Partial | Add attributes to remaining text |
| `user/requestblood.php` | Partial | Add attributes to remaining text |
| `user/becomedonor.php` | ❌ Missing | Add all `data-i18n` attributes |
| `user/donateform.php` | ❌ Missing | Add all `data-i18n` attributes |
| `user/donordashboard.php` | Partial | Add attributes to remaining text |
| `user/profile.php` | Partial | Add attributes to remaining text |
| `user/register.php` | ❌ Missing | Add all `data-i18n` attributes |
| `user/login.php` | Partial | Add attributes to left panel + form |

**Admin pages (5 files)**:
| File | Status | Work needed |
|------|--------|-------------|
| `admin/dashboard.php` | ❌ Missing | Add all `data-i18n` attributes |
| `admin/donors.php` | ❌ Missing | Add all `data-i18n` attributes |
| `admin/hospitals.php` | ❌ Missing | Add all `data-i18n` attributes |
| `admin/requests.php` | ❌ Missing | Add all `data-i18n` attributes |
| `admin/donation_histories.php` | ❌ Missing | Add all `data-i18n` attributes |

### Step 4: Add `data-i18n-placeholder` attributes
For all `<input>` and `<textarea>` elements that have placeholder text but no `data-i18n-placeholder`.

### Step 5: Verify language toggle works on every page
Ensure every page includes:
```html
<script src="../assets/js/translations.js"></script>
<script src="../assets/js/i18n.js"></script>
```
And has a `.lang-toggle-select` dropdown.

## Implementation Order
1. Rewrite `translations.js` with complete, accurate Myanmar translations
2. Edit each PHP file to add missing `data-i18n` and `data-i18n-placeholder` attributes
3. Verify all pages load i18n scripts and language switcher

## Verification
- Open each page in browser
- Toggle language switcher between EN and MY
- Verify all visible text changes language
- Verify placeholders also change
- Check no raw English text remains when MY is selected
